// Example of the "Positive Reframe" Engine
const reframeLogic = (userIssue) => {
  const prompts = {
    lonely: "This is a season of self-discovery. You are preparing for the right people to enter your life.",
    angry: "Your passion is high right now; let's redirect that energy into a win today.",
    stress: "Stress is just a sign that you care deeply. Let's break this into one tiny step."
  };
  
  // Logic to analyze keywords and return an instant positive note
  return instantResponse;
};

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Nature Reframe | Your Digital Sanctuary</title>
    <style>
        :root {
            --forest-green: #2D5A27;
            --leaf-green: #91AC8F;
            --sky-blue: #E3F2FD;
            --earth-brown: #6D4C41;
        }

        body {
            margin: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(180deg, #E3F2FD 0%, #C8E6C9 100%);
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: var(--forest-green);
        }

        .container {
            width: 90%;
            max-width: 400px;
            background: rgba(255, 255, 255, 0.8);
            backdrop-filter: blur(10px);
            padding: 2rem;
            border-radius: 30px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
        }

        h1 { font-size: 1.5rem; margin-bottom: 0.5rem; }
        p { font-size: 0.9rem; color: var(--earth-brown); }

        textarea {
            width: 100%;
            height: 120px;
            border: 2px solid var(--leaf-green);
            border-radius: 15px;
            padding: 1rem;
            margin: 1.5rem 0;
            box-sizing: border-box;
            font-size: 1rem;
            background: white;
            resize: none;
        }

        button {
            background: var(--forest-green);
            color: white;
            border: none;
            padding: 1rem 2rem;
            border-radius: 50px;
            font-weight: bold;
            cursor: pointer;
            transition: transform 0.2s;
        }

        button:active { transform: scale(0.95); }

        #output {
            margin-top: 1.5rem;
            padding: 1rem;
            border-radius: 15px;
            display: none;
            background: white;
            border-left: 5px solid var(--forest-green);
            animation: fadeIn 1s;
        }

        @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }
    </style>
</head>
<body>

<div class="container">
    <h1>Nature Reframe 🌿</h1>
    <p>Let out your stress. Let nature transform it.</p>
    
    <textarea id="userInput" placeholder="I'm feeling lonely because..."></textarea>
    
    <button onclick="transformThought()">Transform My Thought</button>

    <div id="output"></div>
</div>

<script>
    function transformThought() {
        const input = document.getElementById('userInput').value.toLowerCase();
        const outputDiv = document.getElementById('output');
        let response = "";

        if (input.includes("lonely")) {
            response = "🌿 Like a seedling in the soil, being alone is your time to grow deep roots. You are becoming stronger for the forest ahead.";
        } else if (input.includes("angry") || input.includes("mad")) {
            response = "🔥 Anger is just energy. Like a wildfire, it can clear the path for new growth. Take a deep breath; let the smoke clear.";
        } else if (input.includes("stress") || input.includes("tired")) {
            response = "🌊 Even the strongest rivers have calm pools. It is okay to be still. Your worth is not defined by your speed.";
        } else {
            response = "✨ You have the strength of an oak tree within you. This moment is just a passing breeze.";
        }

        outputDiv.innerHTML = `<strong>A Positive Note:</strong><br>${response}`;
        outputDiv.style.display = 'block';
        document.getElementById('userInput').value = ""; // Clears the "bad" thought
    }
</script>

</body>
</html>

npx plugins add vercel/vercel-plugin
